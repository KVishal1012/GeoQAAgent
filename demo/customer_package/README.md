# GeoQA Customer Package Demo

This folder shows the first-revenue GeoQA Data Readiness Audit package for a safe 50-feature Centreline Intersections sample.

Start with `customer_report.md`, then use `issues.csv` for record-level triage and `handoff_bundle.zip` for downstream sharing.
